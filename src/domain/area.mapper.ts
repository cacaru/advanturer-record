import { Area } from "../types/area.type";

export const AreaLabel:Record<string, Area> = {
    "Target": Area.Target,
    "Map": Area.Map,
    "Front": Area.Front,
    "Middle": Area.Middle,
    "Back": Area.Back
}