export interface Resource {
    id: string;
    value: number;
}

export interface ResourceValue {
    value: number;
    id: string;
    name: string;
    image: string;
}

export interface unitData {
    
}